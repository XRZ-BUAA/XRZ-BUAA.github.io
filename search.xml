<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>AlgoNote-Introduction</title>
      <link href="/2024/10/02/Algorithms/AlgoNote-Introduction/"/>
      <url>/2024/10/02/Algorithms/AlgoNote-Introduction/</url>
      
        <content type="html"><![CDATA[<h1 id="初识算法"><a href="#初识算法" class="headerlink" title="初识算法"></a>初识算法</h1><h2 id="基础概念"><a href="#基础概念" class="headerlink" title="基础概念"></a>基础概念</h2><p><strong>算法</strong> 定义良好的计算过程，取一个或一组值作为<strong>输入</strong>，并产生出一个或一组值作为<strong>输出</strong><br><strong>实例 (Instance)</strong> 某一个问题的实例包含了求解该问题所需要的输入（满足有关该问题表述中所给出的任何限制）  </p><p>如果一个算法对其每一个输入实例都能输出正确的结果并<strong>停止</strong>，则称这个算法是正确的  </p><h2 id="形式化定义"><a href="#形式化定义" class="headerlink" title="形式化定义"></a>形式化定义</h2><p><strong>排序问题</strong><br><strong>输入</strong> $n$ 个数 &lt;$a_1,a_2,…,a_n$&gt;<br><strong>输出</strong> 输入序列的一个排列 &lt;$a_1’,a_2’,…,a_n’$&gt;，使得 $a_1’ \leq a_2’ \leq… \leq a_n’$<br>可把待排序的数称为<strong>关键字 (key)</strong>  </p><h2 id="算法的表示——伪代码-Pseudocode"><a href="#算法的表示——伪代码-Pseudocode" class="headerlink" title="算法的表示——伪代码 (Pseudocode)"></a>算法的表示——伪代码 (Pseudocode)</h2><p>伪代码的书写格式按照课程组 PPT 上写的来<br>PPT 中给出的示例——选择排序：  </p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">输入：数组 A[a_1, a_2, ..., a_n]</span><br><span class="line">输出：数组 A[a_1&#x27;, a_2&#x27;, ..., a_n&#x27;]，使得 a_1&#x27; &lt;= a_2&#x27; &lt;= ... &lt;= a_n&#x27;  </span><br><span class="line">for i &lt;- 1 to n-1 do</span><br><span class="line">    cur_min &lt;- A[i]</span><br><span class="line">    cur_min_pos &lt;- i</span><br><span class="line">    for j &lt;- i+1 to n do</span><br><span class="line">        if A[j] &lt; cur_min_pos then</span><br><span class="line">            // 记录当前最小值及其位置</span><br><span class="line">            cur_min &lt;- A[j]</span><br><span class="line">            cur_min_pos &lt;- j</span><br><span class="line">        end</span><br><span class="line">    end</span><br><span class="line">    交换 A[i] 和 A[cur_min_pos]</span><br><span class="line">end</span><br><span class="line">```  </span><br><span class="line">可以从示例中看出伪代码书写的几个要点：  </span><br><span class="line">* 首行定义算法输入和输出</span><br><span class="line">* 语句块缩进 （类似 Python 中的缩进）</span><br><span class="line">* 赋值语句使用 `&lt;-` 符号</span><br><span class="line">* 注释使用 `//` 符号</span><br><span class="line">* 两个变量交换值时，我们不关注交换过程的实现细节</span><br><span class="line">* 循环语句块包含在 `do-end` 语句中</span><br><span class="line">* 条件语句块包含在 `then-end` 语句中</span><br><span class="line">   </span><br><span class="line">## 算法入门  </span><br><span class="line">### 循环不变式  </span><br><span class="line">循环不变式 (Loop Invariant) 可用来帮助我们理解和证明算法的正确性，在循环不变式中，要证明算法的正确性，必须证明它的三个性质：  </span><br><span class="line">* **初始化** 算法在循环的第一轮迭代开始前是正确的</span><br><span class="line">* **保持** 如果在循环的某次迭代开始前它是正确的，那么在下一次迭代开始前它也应该保持正确  </span><br><span class="line">* **终止** 当循环结束时，不变式给了我们一个有用的性质，它有助于表明算法是正确的  </span><br><span class="line">  </span><br><span class="line">### 插入排序  </span><br><span class="line">插入排序算法用于解决排序问题，是一个对**少量**元素进行排序的有效算法  </span><br><span class="line">#### 算法步骤  </span><br><span class="line">插入排序的伪代码 (Pseudocode) 以一个过程的形式给出 —— INSETION-SORT：  </span><br><span class="line">《算法导论》 给出的伪代码如下：  </span><br></pre></td></tr></table></figure><p>INSERTION-SORT(A)<br>    for j &lt;- 2 to length[A]<br>        do key &lt;- A[j]<br>        &#x2F;&#x2F; Insert A[j] into the sorted sequence A[1..j-1]<br>        i &lt;- j - 1<br>        while i &gt; 0 and A[i] &gt; key<br>            do A[i+1] &lt;- A[i]<br>                i &lt;- i - 1<br>        A[i+1] &lt;- key</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">它似乎是认为数组下标从 `1` 开始的？  </span><br><span class="line">按照《算法导论》给出的伪代码写了一个 C++ 实现：  </span><br><span class="line">```CPP</span><br><span class="line"># include &lt;iostream&gt;</span><br><span class="line"></span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">    int arr[] = &#123;5, 2, 4, 6, 1, 3&#125;;</span><br><span class="line">    int n = sizeof(arr) / sizeof(arr[0]);</span><br><span class="line"></span><br><span class="line">    for (int j = 2; j &lt;= n; j++) &#123;</span><br><span class="line">        int key = arr[j];</span><br><span class="line">        int i = j - 1;</span><br><span class="line">        while (i &gt; 0 &amp;&amp; arr[i] &gt; key) &#123;</span><br><span class="line">            arr[i + 1] = arr[i];</span><br><span class="line">            i--;</span><br><span class="line">        &#125;</span><br><span class="line">        arr[i + 1] = key;</span><br><span class="line">    &#125;</span><br><span class="line">    for (int i = 0; i &lt; n; i++) &#123;</span><br><span class="line">        std::cout &lt;&lt; arr[i] &lt;&lt; &quot; &quot;;</span><br><span class="line">    &#125;</span><br><span class="line">    std::cout &lt;&lt; std::endl;</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br><span class="line">```  </span><br><span class="line">程序输出：  </span><br></pre></td></tr></table></figure><p>5 0 1 2 3 4</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">可以看到，该伪代码从实现角度来看并不正确  </span><br><span class="line">  </span><br><span class="line">实现时正确的写法应该是如下的这个版本：  </span><br></pre></td></tr></table></figure><p>INSERTION-SORT(A)<br>    for j &lt;- 1 to length[A]<br>        do key &lt;- A[j]<br>           &#x2F;&#x2F; Insert A[j] into the sorted sequence A[1..j-1]<br>           i &lt;- j - 1<br>           while i &gt;&#x3D; 0 and A[i] &gt; key<br>               do A[i+1] &lt;- A[i]<br>                  i &lt;- i - 1<br>           A[i+1] &lt;- key</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">下面给出该版本伪代码的 C++ 实现：  </span><br><span class="line">```CPP</span><br><span class="line"># include &lt;iostream&gt;</span><br><span class="line"></span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">    int arr[] = &#123;5, 2, 4, 6, 1, 3&#125;;</span><br><span class="line">    int n = sizeof(arr) / sizeof(arr[0]);</span><br><span class="line"></span><br><span class="line">    for (int j = 1; j &lt; n; j++) &#123;</span><br><span class="line">        int key = arr[j];</span><br><span class="line">        int i = j - 1;</span><br><span class="line">        while (i &gt;= 0 &amp;&amp; arr[i] &gt; key) &#123;</span><br><span class="line">            arr[i + 1] = arr[i];</span><br><span class="line">            i--;</span><br><span class="line">        &#125;</span><br><span class="line">        arr[i + 1] = key;</span><br><span class="line">    &#125;</span><br><span class="line">    for (int i = 0; i &lt; n; i++) &#123;</span><br><span class="line">        std::cout &lt;&lt; arr[i] &lt;&lt; &quot; &quot;;</span><br><span class="line">    &#125;</span><br><span class="line">    std::cout &lt;&lt; std::endl;</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br><span class="line">```  </span><br><span class="line">这回输出对了：  </span><br></pre></td></tr></table></figure><p>1 2 3 4 5 6</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">我觉得可以认为，《算法导论》给出的伪代码认为 —— 数组的下标从 `1` 开始。PPT 中给出的伪代码就在首行做出了说明：  </span><br></pre></td></tr></table></figure><p>Input: A[1..n] is an array of numbers<br>for j &lt;- 2 to n do<br>    key &lt;- A[j];<br>    i &lt;- j - 1;<br>    while i &gt;&#x3D; 1 and A[i] &gt; key do<br>        A[i+1] &lt;- A[i];<br>        i &lt;- i - 1;<br>    end<br>    A[i+1] &lt;- key;<br>end</p><pre><code>  #### 循环不变式证明插入排序正确性  ![alt text](image.png)  ## 算法分析  算法分析指对一个算法所需要的资源进行预测  </code></pre>]]></content>
      
      
      <categories>
          
          <category> AlgoNote </category>
          
      </categories>
      
      
    </entry>
    
    
  
  
</search>
